# Dashboard / Stats
- the trends preview shows either last 10 (default) or total. last 10 is not even an option for the stats trends page? bring it back as a view option for stats.


# Live tournament
- it's weird that the select clubs header (that's clickable) has a different color than the section it opens. make the stars/league selection better (keep the random options, but make ui better) -> update everywhere it's used
- the enter comment section does not look like the rest of the app still. consistency!
- matches tab: that uses way too much space now... streamline with rest of app!!!

# Stats
## trends
- zooming/scrolling still does not work at all!! the zooming makes the plot higher and actually i want to zoom in on the x-axis (horziontally, but plot should stay same size overall). so when i zoom in, i want the x-axis to change dude
- see dashboard: bring back last n option
- when win% is selected, it's not clear which view is shown (not highlighted in selection)

## positions
- the cup(s) uses too much space where it is now. dont show in left column, but instead show in top right corner of cell with winner (can be multiple)
- dont show the explanation text


## records
- if there are multiple with the same, show them!

## cups
- for the cups statistics, i want them to re-use the cups stats from the dashboard


# player profile
- view all matches button is very low on screen and on some smarthphones that's a bit over the border
- stats page: improve the representation of the upper half (with the pills for matches played, ...) -> i dont want pills but a nice representation. maybe something like in the stats/player page -> re-use stuff when possible. keep the streaks, they are nice. add the profile net with activity, attack, etc.


# Remarks
plan with opus, let me know when to switch to implementation models. add that it should go over the plan until everything is implemented (maybe use a separate markdown for the plan)