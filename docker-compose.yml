services:
  backend:
    build: ./backend
    volumes:
      - ./backend/data:/data
      - ./backend/secrets.json:/app/secrets.json:ro
      # Optional: external cup config (override path via env CUPS_CONFIG_PATH)
      - ./backend/cups.json:/data/cups.json:ro
    environment:
      DB_URL: "sqlite:////data/app.db"
      CUPS_CONFIG_PATH: "/data/cups.json"
    expose:
      - "8001"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    expose:
      - "80"

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend
      - frontend
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config:
