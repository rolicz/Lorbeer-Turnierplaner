# Gemini CLI entry point

The canonical, tool-agnostic project knowledge lives in **`AGENTS.md`** (stack, commands,
configuration, data model, deployment, backups, conventions, gotchas, current state).
Read it first, and update it when you learn something non-obvious.

`README.md` is the human setup narrative; `REFACTORING_PLAN.md`, `FEATURES_2026-07.md` and
`FEATURES_2026-08.md` are the historical batch trackers.

(This file previously carried an outdated copy of the setup docs — e.g. `editor_password` /
`admin_password` secrets that no longer exist; the backend now uses `player_accounts`.)
