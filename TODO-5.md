# Header/Navbar
- i want the live indicator on the right side instead of left. the indicator should also differentiate between live and connected or just connected (keep other information as well). clickable if live tournament is running and brings me to current match

# Dashboard
- trends overview: it does not span the entire width. use consistent colors for the players lines everywhere, super important!!!

# Live Tournament
## current
- the select clubs and match comments are still super ugly and not user friendly. change how this information is entered and make it consistent with rest of design
## standings
- the standings should have section below with highest potential positions, if everything works out for each player. let me choose the player, reuse elements.
## matches
- use compact by default (might already be, check)
## admin
admin panel is super ugly and not consistent with rest of design

# Stats
## Trends
- trends page is better now, but why did you remove the moving average?? i really liked that -> also use it on dashboard
- with tournaments name, the most recent tournament gets cut off on right side

## Table
i want the option for last n as well, maybe option below? it should work for all the columns i can add and switch all if toggled. n=10 by default, but make it a slider. i think it does not make sense for elo, so unset this if last n is selected. the last n should be the same metric as used by trends for last n.

## Positions
- let me drag player columns around when i drag a player. make sure the lines update dynamically. always show player icons, even if i scroll further down.

## H2H
- dont show explanation text for matrix
- if 2v2 is selected, i want to see stats about how well a player plays with their teamate and similar stuff (add section below)

## player
- in the profile net, let me overlay the net of another player (or multiple) as well. use the correct color for each player (consistent with lines from trends)
- matches list should use consistent list like other parts of the app

## Records
- I dont like that the boxes use a different color than e.g. player stats boxes. like, wtf, this is brighter? i said consistency is key!!!
- also show if there are multiple for longest runs

## Cups
- the cup stats say "1 defended", even though the player only won it and did not play afterwards? so not really defended, is it?

# Remarks
plan with opus, let me know when to switch to implementation models. add that it should go over the plan until everything is implemented (maybe use a separate markdown for the plan)