It's better now, but I still have a few requests.

# nav side bar
show the app icon instead of the generic cup on top of sidebar

# Dashboard
The dashboard shows live now, which looks super ugly as it is so nested in cards. use consistent design like in live tournaments current match. Similar for cups, it's not very nice to view


# Live Tournaments
Select Clubs is ugly. redesign it and make consistent with rest of app. also not quite happy with the match comments section (also add comment).
Standings should use a similar table design as in stats. make sure all information is visible (table headers not selectable here).
Remove the Refresh button on top, only pull down refresh.

# Friendlies
streamline list design with rest of app

# Stats
For trends, make sure the tournament names are readable and dont stop with "...". if a player does not play a tournament, i want to have the line greyed out for that, like it was in the graph before. Metric is fine, but let me combine all metrics with absolute values with "per match" (not two options for each, but just a single modifier "per match").
The zoom should not be a switch: i want to scroll directly in the plot (the plot size stays fixed).

Table is nice, but let me toggle w/d/l with a single toggle. also add GD/M. 

Positions: flip the axis: row for each tournament, players in columns. show full tournament name (use two rows for text if long, but keep height of table rows the same).
Let me toggle a line that connects the laurel owners (use correct colors for tournaments) across the tournament cells. for "cup at stake": use the icons of the according cup that's at stake (can be multiple).

H2H matrix: let me switch different metrics (not only win%, but win% is default).
it's not clear what the top rivalries mean.

Streaks: think about how to use a more consistent representation with the rest of the app

Player: it's not clear what the graph means (right side of player name)?

# Players
going back form player profile to player overview sometimes crashes (not sure if fixed already, i tested while you implemented)


# Remarks
plan with opus, let me know when to switch to implementation models. add that it should go over the plan until everything is implemented (maybe use a separate markdown for the plan)