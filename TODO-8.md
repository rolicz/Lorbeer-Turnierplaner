# Stats
use the new stats as default, but keep the old ones as an option in the settings

## Trends
with enabled tournament names, the y axis is moved to the right, so there is wasted space. just let me scroll a bit further to let me read the first tournament name fully, but keep the y-axis on the left
