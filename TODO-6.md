# General
last N should use 5 as default

# Live Tournament
- when creating a tournament, there is always already a comment (e.g. 2' 0-1 A), maybe that is a leftover and not an issue in practice?
- collapse select clubs by default if a club is already selected. uncollapse match comments

## standings
- make the player selection for best-case position like other player selections (clicking on icon, not a dropdown). i cant believe you still dont use consistent design elements!!!
- best-case: i hope it calculates the other games as well for that? for example, i chose berni with (out of 3 players) and now i see berni 6pts, flo 0 pts, roli 0pts -> how would that be possible? flo+roli have to play as well, so at least 1 pts each or one gets 3?

# Stats
## Trends
- the tournament names are super fucked now, they overlap in the center. just keep it rotated (from left bottom to right top, so the most recent never gets cut off). let me scroll so far that i can see the first tournament name fully
- i also want the greyed out line for last n view (as it already is in dashboard)

## cups
- it's a bit weird that it says "Atzi took it from Berni" but then it shows streak 1 and it's not clear who the streak belongs to. improve this (also on dashboard cups page)

# Remarks
plan with opus, let me know when to switch to implementation models. add that it should go over the plan until everything is implemented (maybe use a separate markdown for the plan)