# Dashboard

- when clicking on a tournament and going back, i want to get to the correct dashboard page again.
- trends: show a meaningful stat that is also an option in the stats. clicking on it should bring me to that trends chart in the statistics page

# Tournaments view
organize a bit better, for example separated by months

# Live Tournaments
i dont lke the new select clubs and math comment section, the collapsible option is way too small. if this is re-used somewhere else, fix everywhere

i dont like the new standings table, as is scrolls out of screen even on large smartphones. fix that (you could use multiple rows for the player details on the left side for example, they take a lot of space)

# stats
## trends view
the tournament names should not make the plot smaller.
the plot is still not scrollable and zoomable -> when i drag my fingers in the plot, i want horizontal zoom.
## table
gf/ga/gd should also be just one option (and as gf/m, ga/m and gd/m)
## positions
- the first cup line (should be yellow/gold) is not visible.
- save some horizontal space by writing tournament names across mulitple lines if they are longer -> keep cell height the same for all rows though.
- make the cup indicators not part of the tournament name, but show somewhere where it fits nicely
- cup lineage should be shown always (not optional)
## h2h
- matrix: add option for w/d/l
## stars
- streamline design to be more consistent with rest of app
## player
player profiles should be clickable -> make sure back works

## clubs
make clubs list consistent with rest of design of app


# Remarks
plan with opus, let me know when to switch to implementation models. add that it should go over the plan until everything is implemented (maybe use a separate markdown for the plan)